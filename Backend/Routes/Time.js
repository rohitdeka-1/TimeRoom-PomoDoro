/**
 * @description- This Route is for Time
 * @todo- Need to Implement this routes
    POST /rooms/:id/start (host starts)
    POST /rooms/:id/pause
    GET /rooms/:id/status (time left, current state)
    Socket events: timer-sync, timer-control, etc.
*/


